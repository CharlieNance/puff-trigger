from fastapi import APIRouter

router = APIRouter()


# TODO: GET /status — sensor status and latest reading
# TODO: GET /sounds — list available audio clips
# TODO: POST /play/{filename} — manually trigger a clip
# TODO: GET /logs — retrieve log entries
# TODO: POST /settings — update trigger threshold and cooldown
